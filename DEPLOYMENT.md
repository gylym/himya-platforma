# Негізгі сайтқа енгізу

Жаңартулар қолданыстағы `gylym/himya-platforma` және Supabase `ekseiqhgrgifqmpnfzul` жобаларына арналған.

## Қолданыстағы архитектура

GitHub Pages тек HTML/CSS/JavaScript таратады. Қолданушылар Supabase Auth-та, материалдар PostgreSQL-де, файлдар Supabase Storage-та сақталады. `config.js` тек жариялауға арналған `sb_publishable_...` кілтін қамтиды. Бұл серверлік құпия кілт емес. `service_role` тек Supabase Edge Function ортасында қолданылады.

## Қолданыстағы жобаға жаңарту

1. GitHub және Supabase жобасының басқаруына кіру керек. Дерекқор мен Storage-тың қолданыстағы резервтік көшірмесінің барын тексеріңіз.
2. Кодты жеке тармаққа енгізіп, GitHub Actions тексерулерін орындаңыз. Алдымен жаңартылған frontend-ті орналастыруға болады: қолданыстағы файлдарды жаңа signed URL тәсілімен ашу ескі қоймада да тексерілді. Жаңа metadata редакторы миграция орындалған соң жұмыс істейді.
3. Қолданыстағы Supabase жобасына **тек жаңа** миграцияларды ретімен бір рет қолданыңыз: `supabase/migrations/202609130001_platform_repair.sql`, одан кейін `supabase/migrations/202609130002_auth_consistency.sql`. Екінші миграция email өзгергендегі логин сәйкестігін және соңғы әкімшіні қорғауды бекітеді. Бұрынғы `seed.sql`, `db reset` немесе бастапқы миграцияларды өндірістік базаға қайта орындамаңыз. Жаңа миграция файлдарды өшірмейді, профильдерге логин, файлдарға жеке метадеректер, жариялау саясаты және қысқа сілтеме рұқсатын қосады. Алғашқы екі миграция қойманың public күйін өзгертпейді. Signed URL қолдайтын frontend жарияланғаннан кейін ғана `202609130003_private_storage.sql` миграциясын орындаңыз: осы сәттен бастап ескі тұрақты public сілтемелер тоқтайды.
   Одан кейін `202609130004_quiz_visibility.sql` қолданылады: жасырын бөлімнің тестін RPC арқылы тапсыруға тыйым салады.
4. `supabase/functions/username-login` функциясын осы жобаға орналастырыңыз. `supabase/config.toml` бұл функция үшін `verify_jwt=false` қояды, өйткені кіру алдында қолданушыда JWT болмайды. Функцияның өзі парольді Supabase Auth арқылы тексереді; логинге 15 минутта 10 әрекет шектеуі қойылған. `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` тек функция ортасында болады. `ALLOWED_ORIGINS=https://gylym.github.io`.
5. Supabase Auth URL Configuration-де Site URL мәні `https://gylym.github.io/himya-platforma/` болсын. Email растау хаттарының жеткізілуін тексеріңіз. Email растауы өшірілмейді. Расталғаннан кейін қолданушы кіру формасын ашады.
6. Бұрынғы аккаунттар email арқылы кіреді. Жаңа тіркелушілер бірегей логин таңдайды. Бұрынғы аккаунтқа логин беру қажет болса, жоба иесі `profiles.username` өрісіне бірегей 3–32 таңбалы латын әрпі/сан/_ мәнін тағайындай алады.
7. Жарияланған нұсқада жаңа оқушы және әкімші аккаунттарымен төмендегі сценарийді қайталаңыз.

Frontend, SQL және Edge Function бір релиз ретінде енгізілуі керек. SQL-ді қолданбай тек HTML/CSS жариялау тапсырманы аяқтамайды.

## Өндірістік қабылдау сценарийі

Тіркелу → email растау → email және логинмен кіру → жаңарту → материал іздеу → PDF ашу → DOCX/PPTX жүктеу → шығу → әкімшімен кіру → Dashboard → файл қосу → жариялау → бір файлдың атауын өзгерту → жасыру → оқушыдан файл метадеректері мен жаңа signed URL алудың бұғатталуы → сынақ материалын өшіру → оқушының тікелей API арқылы админ әрекеттерін жасай алмайтынын растау.

Жаңа signed URL 60 секундқа жарамды. Жасырудан бұрын берілген сілтеме осы мерзім біткенше жұмыс істеуі мүмкін; жаңа сілтеме алу Storage RLS арқылы бұғатталады. Бұрын жүктеп алынған файлды қашықтан қайтарып алу мүмкін емес.

## Жергілікті тексеру

Python 3.11+ және Node.js 24:

```sh
python3 tests/integration.py
python3 tests/security.py
PYTHONPATH=. python3 tests/presentation.py
node --test tests/platform.mjs
node tests/store.mjs
```

`python3 tests/smoke.py` үшін `./start.sh` сервері бөлек іске қосылады. Құпия айнымалыларды қауіпсіз орта арқылы орнатыңыз. Локалды әкімші тек `INITIAL_ADMIN_PASSWORD` айнымалысы берілген кезде жасалады; дайын ортақ пароль жоқ. Локалды SQLite және жүктелген файлдар `data/` ішінде сақталады, Git-ке кірмейді. Локалды серверде HttpOnly cookie қолданылады; сервер файлдары мен дерекқор HTTP арқылы берілмейді.


## 2026-09-13 өндірістік тексеру

PR #1 және #2 негізгі тармаққа біріктірілді, GitHub Pages жариялауы сәтті аяқталды. 001–004 миграциялары және username-login функциясы енгізілген. Дерекқорда транзакцияны ROLLBACK арқылы аяқтайтын 49 қауіпсіздік/CRUD/quiz тексеруі өтті (49 PASS, 0 FAIL). 32 бөлім мен 62 бастапқы файл сақталды.

**Ашық тіркелудің қалған тәуелділігі:** Supabase Authentication → Emails → SMTP Settings-де custom SMTP өшірулі. Әдепкі SMTP тек ұйым командасының email-деріне жібереді. Қоғамдық тіркелуді аяқтау үшін иесінің пошта қызметін және жіберуші доменін байланыстыру керек. Email растауын айналып өтуге болмайды. Растау хаты → шынайы кіру сценарийі әлі өндірісте толық тексерілген жоқ.

Chrome автоматтандыруының файл таңдау рұқсаты болмағандықтан жаңа файлды форма арқылы жүктеу толық қабылдау тексеруінен өткен жоқ. Браузер download оқиғасын да растай алмады. Файлдар қауіпсіз сақтау мен signed URL арқылы ашылуға бейімделген; бұл шектеулерді толық орындалды деп белгілемеу керек.
